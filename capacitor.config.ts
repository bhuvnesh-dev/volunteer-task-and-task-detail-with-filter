import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'volunteer-task-detail-with-filter',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
